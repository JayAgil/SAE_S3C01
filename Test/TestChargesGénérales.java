import static org.junit.Assert.*;
import org.junit.Test;
import modele.BienLouable;
import modele.ChargesGenerales;

public class TestChargesGénérales {

    //@Test
    //public void testChargesGenerales_getters() {
    //    BienLouable bl = new BienLouable(null, null, null, 0, 0, null, null, null);
      //  ChargesGenerales charge = new ChargesGenerales("id1", "type", 100.0, 0.1f, 50.0, "janvier", bl);
//
  //      assertEquals("id1", charge.getIdChargesGenerales());
    //    assertEquals("type", charge.getTypeCharge());
      //  assertEquals(100.0, charge.getMontant(), 0.001);
        //assertEquals(0.1f, charge.getPourcentage(), 0.001);
        //assertEquals(50.0, charge.getQuotite(), 0.001);
        //assertEquals("janvier", charge.getMois());
        //assertEquals(bl, charge.getBienLouable());
    //}
}
